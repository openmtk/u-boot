if TARGET_COREBOOT_ARM_64BIT

config TEXT_BASE
	default 0x50000000

config BOARD_SPECIFIC_OPTIONS # dummy
	def_bool y
endif

config PRE_CON_BUF_ADDR
	hex
	default 0x40100000

if TARGET_QEMU_ARM_64BIT && !TFABOOT

config BOARD_SPECIFIC_OPTIONS
	imply SYS_MTDPARTS_RUNTIME
	imply SET_DFU_ALT_INFO

endif

